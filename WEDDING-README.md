# 婚禮網站版本說明

這份網站由原本的「2026 Momo's Day 生日站」改造成 **Ethan & Momo 婚禮版**。
以下整理「哪裡改設定」與「哪些內容要換成你們自己的」。

## 🔧 改名字 / 日期 / 地點 / 密碼 → 只改一個檔

打開 **`js/config.js`**，裡面集中了：

| 設定 | 說明 |
|---|---|
| `groom / bride / couple` | 新郎新娘名字（目前是假名 **Ethan・宥辰 ＆ Momo・沐晴**）|
| `date / dateISO / venue / city` | 婚禮日期、地點 |
| `hashtags` | 大廳的標籤 |
| `password` | 進入「新人專屬信箱」的密碼（預設 `1010`）|
| `ownerKey` | 網址加這個 hash（預設 `#couple`）才會出現新人信箱入口 |

> 註：各頁**標題**上的名字是靜態文字，config.js 旁已加註解；要改標題直接改該 HTML 也可以。

## 🎨 配色主題

已改成 **香檳米白金（champagne）** 為預設，右下角 🎨 可切換 4 種婚禮色系：
香檳金 / 霧玫瑰 / 鼠尾草綠 / 霧霾藍。色票在 `css/common.css` 最上方。

## 🎵 背景音樂

右下角 🔇 播放**婚禮進行曲**（華格納〈婚禮合唱〉音樂盒版，純前端合成，無音檔）。

## 📝 待換成你們自己的內容（目前沿用原作範例）

| 頁面 | 檔案 | 要換什麼 |
|---|---|---|
| 戀愛時光（時間軸）| `js/exhibition.js` 的 `ITEMS` | 換成你們的戀愛里程碑（相遇→交往→求婚→結婚）與照片 |
| 婚禮小卡（抽卡）| `js/draw.js` 的 `CARDS` | 換成你們的婚紗照 / 生活照 |
| 甜點桌 | `js/cake.js` 的 `CAKES` | 甜點清單（現為通用甜點，可留可換）|
| 新人小測驗 | `js/quiz.js` 的題庫 | 換成關於你們的題目（目前題目關於新娘 Momo）|
| 照片 / 場景圖 | `images/` | 大廳、場景、卡片、時間軸照片 |

## ⚠️ Firebase（祝福牆 / 信箱 / 抽卡的雲端儲存）

`js/firebase-init.js` 目前仍指向原生日站的專案 `birthday-mo2026`。
**建議另開一個 Firebase 專案**、換掉該 config，避免婚禮資料和生日站混在一起。
（不換也能跑，只是共用同一個資料庫。）

## 🆕 婚禮還可以再加的功能（尚未實作）

RSVP 出席回覆、婚禮資訊卡＋地圖、當日流程表、座位查詢、婚禮日期倒數計時、賓客共筆相簿、電子禮金連結。
